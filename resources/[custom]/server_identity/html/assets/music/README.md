# Loading screen music

1. Drop a royalty-cleared `.mp3` or `.ogg` here (e.g. `track.mp3`).
2. In `../config.js`, set `music.enabled` to `true` and match `music.file`.
3. Restart `server_identity` (or the server) so the file is re-streamed.
