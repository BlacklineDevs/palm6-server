{
  "name": "palm6-lua-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for the pure Lua logic in palm6 resources. Dev tool only: no resource depends on this and nothing here is loaded by the server.",
  "scripts": {
    "test": "node run.js"
  },
  "devDependencies": {
    "fengari": "^0.1.5"
  }
}
